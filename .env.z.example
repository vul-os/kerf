# .env — Vite frontend configuration ONLY.
#
# Copy to `.env` (default), `.env.dev`, or `.env.main` per Vite's mode.
# The backend has moved to kerf.toml (see kerf.example.toml at the repo
# root). Anything other than VITE_* keys here is ignored.

# URL of the backend API. In dev, Vite proxies /api and /auth to this.
VITE_API_URL=http://localhost:8080

# Google OAuth client ID for the browser-side OAuth start. Must match
# auth.google.client_id in kerf.toml.
VITE_GOOGLE_CLIENT_ID=

# Cloud Beta mode: when set to "true" (or "1" / "yes"), billing and tier-change
# controls are visibly disabled in the UI — all product features remain
# accessible and everyone is effectively on the Free tier. The backend also
# enforces this via CLOUD_BETA=true (set it in the server environment alongside
# CLOUD_ENABLED=true; it is mirrored to the frontend through /api/config so
# either signal suffices — setting one is enough).
# VITE_CLOUD_BETA=
