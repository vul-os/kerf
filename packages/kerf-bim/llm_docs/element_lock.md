# element_lock

*Module: `kerf_bim.tools.element_lock` · Domain: bim*

> **Note:** Tools ['bim_request_lock', 'bim_release_lock', 'bim_extend_lock', 'bim_list_locks', 'bim_cleanup_expired_locks'] defined in source but not in Registry — likely require an optional dependency.

## See also

- Package: `kerf_bim`
