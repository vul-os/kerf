name: Publish kerf-sdk (PyPI)

# Publishes packages/kerf-sdk to PyPI via OIDC Trusted Publishing — no
# API token is stored anywhere. Fires on sdk-py-v* tags (parallel to the
# Rust SDK's sdk-rs-v* scheme). These tags do NOT match release.yml's
# `v*` trigger, so a SDK release never kicks off the Docker release.
#
# One-time PyPI setup (pending publisher — works before the project
# exists, independent of any PyPI organization):
#   https://pypi.org/manage/account/publishing/
#     PyPI Project Name : kerf-sdk
#     Owner             : kerf-sh
#     Repository name   : kerf
#     Workflow name     : publish-sdk-py.yml
#     Environment name  : (leave blank)
#
# Cut a release:
#   git tag sdk-py-v0.1.0 && git push origin sdk-py-v0.1.0

on:
  push:
    tags:
      - 'sdk-py-v*'

permissions:
  contents: read

jobs:
  publish:
    name: Build + publish kerf-sdk to PyPI
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # required for PyPI Trusted Publishing (OIDC)
      contents: write   # required to create the GitHub Release
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build sdist + wheel
        run: |
          python -m pip install --upgrade build
          python -m build packages/kerf-sdk --outdir packages/kerf-sdk/dist

      - name: Publish to PyPI (Trusted Publishing / OIDC)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/kerf-sdk/dist

      # Same workflow also cuts a GitHub Release for the sdk-py-v* tag and
      # attaches the built sdist + wheel. This release is SEPARATE from the
      # app's `v*` release (release.yml) and carries no SHA256SUMS manifest and
      # no build-provenance attestation: the authoritative distribution channel
      # for the SDK is PyPI, which records its own hashes, and these assets are
      # a convenience copy. Do not tell users to verify them with
      # scripts/verify.sh — it would (correctly) find no manifest and refuse.
      - name: Create GitHub Release with artifacts
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: kerf-sdk ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            packages/kerf-sdk/dist/*.tar.gz
            packages/kerf-sdk/dist/*.whl
